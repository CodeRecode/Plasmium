#pragma once

namespace Plasmium {
    enum class FactionType {
        Player,
        Monster,
        FactionCount
    };

    enum class ActionType {
        Attack,
        Bump,
        Idle,
        Move,
        ActionTypeCount
    };
}